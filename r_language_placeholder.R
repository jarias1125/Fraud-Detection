# r_language_placeholder.R
# This is a placeholder file to help GitHub detect R as the primary language.

# Simple R function
add_numbers <- function(a, b) {
  return(a + b)
}

# Example usage
result <- add_numbers(5, 3)
print(result)
